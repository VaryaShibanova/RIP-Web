<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Аномальные паттерны в дендрошкале</title>
    <link rel="stylesheet" href="/resources/style.css?v=3">
</head>
<body>
    <div class="header-nav">
        Аномальные паттерны
    </div>
    
    <h1 class="main-title">аномальные паттерны в дендрошкале</h1>
    
    <div class="search-container">
        <form method="GET" action="/anomalies">
            <div class="search-box"></div>
            <input type="text" name="findanomalies" class="search-input-field" placeholder="Поиск" value="{{.query}}"> <!-- Исправлено на findanomylies -->
            <button type="submit" class="search-icon">
                <img src="http://127.0.0.1:9000/images/img/search-icon.png" alt="Поиск">
            </button>
        </form>
        {{if gt .cart_count 0}}
        <a href="/tree/1" class="tree-icon">
            <img src="http://127.0.0.1:9000/images/img/user-icon.jpg" alt="Дерево">
            <span class="cart-count">{{.cart_count}}</span>
        </a>
        {{else}}
        <div class="tree-icon disabled">
            <img src="http://127.0.0.1:9000/images/img/user-icon.jpg" alt="Дерево">
        </div>
        {{end}}
    </div>

    <div class="container">
        <div class="anomalies-grid">
            {{range .anomalies}}
            <div class="anomaly-card">
                <a href="/anomaly/{{.ID}}" class="anomaly-link">
                    <img src="{{.Image}}" alt="{{.Name}}">
                    <h3>
                        <span>{{.Name}}</span>
                        <form method="POST" action="/tree/add" class="add-to-tree-form">
                            <input type="hidden" name="anomaly_id" value="{{.ID}}">
                            <input type="hidden" name="anomalous_rings" value="{{.Year}}-{{add .Year 1}}">
                            <input type="hidden" name="calculated_year" value="{{.Year}}">
                            <button type="submit" class="add-to-tree-btn" title="Добавить в дерево">
                                <img src="http://127.0.0.1:9000/images/img/add-b.png" alt="Добавить">
                            </button>
                        </form>
                    </h3>
                    <div class="year">Год начала: {{.Year}} г.</div>
                    <p><strong>Описание:</strong> {{.Description}}</p>
                </a>
            </div>
            {{end}}
        </div>
    </div>
</body>
</html>